name: delete_project
server: codebase-memory
category: graph-analysis
description: Delete a project from the index
inputSchema:
  type: object
  properties:
    project:
      type: string
  required:
    - project
example: |-
  await codebase_memory["delete_project"]({
    project: "<project>"
  });
