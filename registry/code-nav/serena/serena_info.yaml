name: serena_info
server: serena
category: code-nav
description: Retrieves Serena-specific information.
inputSchema:
  type: object
  properties:
    topic:
      title: Topic
      type: string
      description: The topic, which you must have been given explicitly.
  required:
    - topic
  title: applyArguments
example: |-
  await serena["serena_info"]({
    topic: "<topic>"
  });
