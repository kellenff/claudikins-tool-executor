name: delete_memory
server: serena
category: code-nav
description: Delete a memory, only call if instructed explicitly or permission was granted by the user.
inputSchema:
  type: object
  properties:
    memory_name:
      title: Memory Name
      type: string
  required:
    - memory_name
  title: applyArguments
example: |-
  await serena["delete_memory"]({
    memory_name: "<memory_name>"
  });
