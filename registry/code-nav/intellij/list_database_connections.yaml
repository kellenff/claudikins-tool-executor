name: list_database_connections
server: intellij
category: code-nav
description: Retrieves a list of configured database connections or data sources in the project.
inputSchema:
  type: object
  properties:
    projectPath:
      type: string
      description: |2-
         The project path. Pass this value ALWAYS if you are aware of it. It reduces numbers of ambiguous calls. 
         In the case you know only the current working directory you can use it as the project path.
         If you're not aware about the project path you can ask user about it.
  required: []
example: |-
  await intellij["list_database_connections"]({
    projectPath: "<projectPath>"
  });
