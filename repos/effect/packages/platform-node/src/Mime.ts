/**
 * Re-exports the `mime` package through the `@effect/platform-node/Mime` module and the `Mime`
 * namespace in the package barrel.
 *
 * @since 4.0.0
 */

/* oxlint-disable no-named-as-default */

/** @since 4.0.0 */
import Mime from "mime";

/**
 * @since 4.0.0
 * @category Re-exports
 */
export * from "mime";

/**
 * @since 4.0.0
 * @category Re-exports
 */
export default Mime;
