/**
 * Re-exports the `find-my-way-ts` router package used by the unstable HTTP routing modules.
 *
 * This module keeps the router types and helpers available from the Effect HTTP namespace without
 * wrapping or changing them.
 *
 * @since 4.0.0
 */

/**
 * @since 4.0.0
 * @category Re-exports
 */
export * from "find-my-way-ts";
