/**
 * Re-exports the Web multipart helpers from `multipasta`.
 *
 * This module keeps the Web-specific parser helpers available from the Effect HTTP namespace
 * without wrapping or changing them.
 *
 * @since 4.0.0
 */

/**
 * @since 4.0.0
 * @category Re-exports
 */
export * from "multipasta/web";
